{
  "defaultAccount": "azienda_a",
  "bindingMode": "global",
  "accounts": [
    {
      "id": "azienda_a",
      "service": "mock",
      "label": "Azienda A (mock)",
      "command": "node",
      "args": ["mock-upstream/server.mjs"],
      "env": { "MOCK_ACCOUNT": "azienda_a" }
    },
    {
      "id": "azienda_b",
      "service": "mock",
      "label": "Azienda B (mock)",
      "command": "node",
      "args": ["mock-upstream/server.mjs"],
      "env": { "MOCK_ACCOUNT": "azienda_b" }
    }

    // --- Esempio reale Supabase (un account per cliente) ---
    // ,{
    //   "id": "cliente_x_supabase",
    //   "service": "supabase",
    //   "label": "Cliente X — Supabase",
    //   "command": "npx",
    //   "args": ["-y", "@supabase/mcp-server-supabase@latest", "--read-only", "--project-ref=REF_X"],
    //   "env": { "SUPABASE_ACCESS_TOKEN": "${SUPABASE_PAT_CLIENTE_X}" }
    // }
  ]
}
